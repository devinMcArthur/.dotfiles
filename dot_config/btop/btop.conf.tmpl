#? Config file for btop v. 1.4 — chezmoi-managed subset. btop rewrites this
#? file with the full option set when settings change in-app; the two options
#? below are the ones we pin (chezmoi will show that drift — re-add here if
#? an in-app change should become permanent).

color_theme = "{{ .theme.btop }}"

#* Transparent background — let ghostty's opacity show through.
theme_background = False
