# Plugins for zsh, loaded by antidote.
# https://getantidote.github.io/
# Format: <github-user>/<repo> [path:<subdir>] [kind:<zsh|fpath|defer>]

# --- git aliases (cherry-picked from oh-my-zsh, no framework) ---
# Note: ssh-agent plugin removed in favor of 1Password's SSH agent
# (single stable socket at ~/.1password/agent.sock; SSH_AUTH_SOCK export
# lives in dot_zshrc.tmpl). The plugin's per-shell spawn-or-attach logic
# races on parallel tmux pane restores after reboot, prompting for the
# key passphrase in every pane.
ohmyzsh/ohmyzsh path:plugins/git

# --- quality of life ---
zsh-users/zsh-completions                     kind:fpath
zsh-users/zsh-history-substring-search

# --- must come LAST among interactive plugins ---
zsh-users/zsh-autosuggestions
zsh-users/zsh-syntax-highlighting
